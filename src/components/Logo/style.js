import styled from "styled-components"

export const LogoStyle = styled.div`

  img {
    margin-bottom: 2rem;
    height: 15rem;
    filter: invert(97%) sepia(3%) saturate(0%) hue-rotate(288deg) brightness(103%) contrast(105%);
    opacity: 0.1;
  }

`
